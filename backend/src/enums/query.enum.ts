export enum Queries {
  fromDate = 'fromDate',
  toDate = 'toDate',
  page = 'page',
  limit = 'limit',
  type = 'type',
  minAmount = 'minAmount',
  maxAmount = 'maxAmount',
  category = 'category',
}
